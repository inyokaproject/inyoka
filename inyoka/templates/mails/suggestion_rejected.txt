{% trans username=username, title=title, note=note|wordwrap(75, false)|indent(4), domain=domain -%}
Hello,

{{ username }} rejected your article suggestion “{{ title }}”. The specified reason is:

    {{ note }}

Kind regards,

Your {{ domain }} team
{%- endtrans %}